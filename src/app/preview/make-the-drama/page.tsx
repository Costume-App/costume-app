import { LandingPage } from "@/components/landing/LandingPage";

export const metadata = {
  title: "Make the Drama — play production management",
  description: "Your all-in-one play production management suite — casts, costumes, fabric, and budget in one place.",
};

export default function MakeTheDramaPreview() {
  return <LandingPage slug="make-the-drama" />;
}
