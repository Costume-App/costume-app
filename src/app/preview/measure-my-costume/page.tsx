import { LandingPage } from "@/components/landing/LandingPage";

export const metadata = {
  title: "Measure My Costume — costume & production management",
  description: "Every costume, every cast member, every yard — in one place.",
};

export default function MeasureMyCostumePreview() {
  return <LandingPage slug="measure-my-costume" />;
}
